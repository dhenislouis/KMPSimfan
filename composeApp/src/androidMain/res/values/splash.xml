<!-- styles.xml -->
<resources>
    <!-- Tema untuk SplashScreen -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FFFFFF</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_simfan</item>
        <item name="postSplashScreenTheme">@style/Theme.App.NoActionBar</item>
    </style>

    <!-- Tema utama AppCompat tanpa AppBar -->
    <style name="Theme.App.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Tambahkan warna, typografi, dll jika perlu -->
    </style>
</resources>